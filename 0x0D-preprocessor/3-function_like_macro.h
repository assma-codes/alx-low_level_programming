#ifndef MACRO_HEADER
#define MACRO_HEADER
/**
 * File: 3-function_like_macro.hi
 *
 * Desc: haeder file
 * X: variable for which get the absolute value
 */
#define ABS(X) ((X) < 0 ? -(1) * (X) : (X))

#endif
